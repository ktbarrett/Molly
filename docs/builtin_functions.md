# Builtin Functions

These are regularly evaluated functions that either require special runtime support or are so common as to be already provided.
Like the Syntactic Forms, these function values exist in the `builtin` namespace, below the `global` namespace.

## `input`

## `print`
